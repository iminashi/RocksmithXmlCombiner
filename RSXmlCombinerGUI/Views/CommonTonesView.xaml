<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:views="clr-namespace:RSXmlCombinerGUI.Views"
             xmlns:vm="clr-namespace:RSXmlCombinerGUI.ViewModels"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             Width="700"
             x:Class="RSXmlCombinerGUI.Views.CommonTonesView">
  <Design.DataContext>
    <vm:CommonTonesViewModel/>
  </Design.DataContext>
  
  <StackPanel Spacing="10" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center">
    <ItemsControl x:Name="ArrangementsList">
      <ItemsControl.ItemsPanel>
        <ItemsPanelTemplate>
          <WrapPanel Orientation="Horizontal"/>
        </ItemsPanelTemplate>
      </ItemsControl.ItemsPanel>

      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <views:ToneNamesView/>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>

    <Button x:Name="OkButton"
            Content="OK"
            FontSize="20"
            HorizontalAlignment="Center"
            Width="140"/>
  </StackPanel>
</UserControl>
